// GENERATED BY SCHEMOCK - DO NOT EDIT
// Client methods for custom endpoints

import type * as Types from './types';

// API base URL - configure based on environment
const API_BASE = typeof window !== 'undefined' ? window.location.origin : '';

export const endpoints = {
  authhubUsers: async (params: Types.AuthhubUsersParams): Promise<Types.AuthhubUsersResponse> => {
    const url = new URL(`${API_BASE}/api/authhub/users`);
    if (params.search !== undefined) {
      url.searchParams.set('search', String(params.search));
    }
    if (params.limit !== undefined) {
      url.searchParams.set('limit', String(params.limit));
    }
    if (params.offset !== undefined) {
      url.searchParams.set('offset', String(params.offset));
    }
    const response = await fetch(url.toString());

    if (!response.ok) {
      const error = await response.json().catch(() => ({}));
      throw new Error(error.message || `HTTP ${response.status}`);
    }

    return response.json();
  },

  authUserPermissions: async (): Promise<Types.AuthUserPermissionsResponse> => {
    const response = await fetch(`${API_BASE}/api/auth/user-permissions`, { method: 'GET' });

    if (!response.ok) {
      const error = await response.json().catch(() => ({}));
      throw new Error(error.message || `HTTP ${response.status}`);
    }

    return response.json();
  },

  authInviteUser: async (params: Types.AuthInviteUserParams): Promise<Types.AuthInviteUserResponse> => {
    const response = await fetch(`${API_BASE}/api/auth/invite-user`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(params),
    });

    if (!response.ok) {
      const error = await response.json().catch(() => ({}));
      throw new Error(error.message || `HTTP ${response.status}`);
    }

    return response.json();
  },

};